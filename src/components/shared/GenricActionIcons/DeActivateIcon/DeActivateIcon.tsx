import React, { FC } from 'react';
import { IconBaseProps } from 'react-icons';
import { ImEyeBlocked } from 'react-icons/im';

const DeActivateIcon: FC<IconBaseProps> = (props) => {
  return <ImEyeBlocked {...props} />;
};

export default DeActivateIcon;
